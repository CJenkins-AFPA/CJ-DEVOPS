# Monitoring

- Traefik dashboard (api@internal) : `https://traefik.local` (via router dédié).
- Portainer : `https://portainer.local` pour observer services et logs.
- Ajouter (optionnel) Prometheus/Grafana si nécessaire.
