name: Mirror all refs (branches, tags, notes) from this repo (CJenkins-AFPA/CJ-DEVOPS)
     to uyoop/DevOpsTraining. Requires repository secret TARGET_PAT in the source repo.
     
name: Mirror to uyoop/DevOpsTraining

on:
  push:
    branches: ['**']
  schedule:
    - cron: '0 8 * * *'  # daily at 08:00 UTC
  workflow_dispatch:

concurrency:
  group: mirror-to-uyoop
  cancel-in-progress: true

jobs:
  mirror:
    runs-on: ubuntu-latest
    env:
      TARGET_REPO: uyoop/DevOpsTraining
      TARGET_PAT: ${{ secrets.TARGET_PAT }}
    steps:
      - name: Check secret period has not expired
        run: |
          EXPIRY="2026-06-30"
          TODAY=$(date -u +%Y-%m-%d)
          if [[ "$TODAY" > "$EXPIRY" ]]; then
            echo "Mirror period expired on $EXPIRY (today: $TODAY). Exiting without pushing."
            exit 0
          fi
        
      - name: Checkout repository (no persisted credentials)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Sanity check TARGET_PAT
        run: |
          if [[ -z "$TARGET_PAT" ]]; then
            echo "TARGET_PAT is not set or is empty. Please add the secret TARGET_PAT in repository settings."
            exit 1
          fi
          echo "TARGET_PAT length: ${#TARGET_PAT}"

      - name: Configure git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Add target remote with authentication
        run: |
          git remote add target https://oauth2:${TARGET_PAT}@github.com/${TARGET_REPO}.git
          git remote -v

      - name: Mirror push to target
        run: |
          git push target --mirror --force
